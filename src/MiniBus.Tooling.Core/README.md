# MiniBus.Tooling.Core

Provider-neutral contracts for MiniBus operational tooling.

This package defines read models, filters, timeline fragments, reader interfaces, and explicit action contracts that can be shared by CLI, API, and future UI front doors. It intentionally has no SQL, Azure Service Bus, logging, or UI dependencies.
